# ESP32 空调控制器项目问题总结

> 本文按当前仓库代码整理，重点记录已经解决的问题、仍需注意的边界和现场排查方法。历史版本中的发布脚本、GitHub 自动推送、ESP8266 流程和个人项目文案不属于当前项目问题范围。

## 一、当前项目基线

### 当前主线

- 推荐编译环境：`esp32_wifi`
- 固件版本：`1.5.31`
- 主功能：WiFi 配网、MQTT 控制、Apple HomeKit、红外发射/学习、AHT20 温湿度、定时任务、WiFi OTA
- WiFi 仅支持 2.4 GHz
- OTA 使用双应用分区，清单文件为 `firmware/ota_esp32_wifi.json`

### 兼容功能

- BLE 代码仍保留，微信小程序和 BLE 协议文档仍可用于兼容设备。
- `esp32dev` 是包含 BLE 和 WiFi/HomeKit 的兼容构建环境，默认从 BLE 模式启动，可切换到 WiFi。
- `esp32_ble` 是不编译 WiFi/MQTT 的 BLE-only 构建环境。
- `esp32_wifi` 固件不启用 BLE 协议栈，但仍保留 `BleManager` 的命令处理入口，因为 MQTT 会复用 `BleManager::handleCommand()`。

### 当前硬件引脚

| 功能 | 引脚 | 说明 |
| --- | --- | --- |
| 红外发射 | GPIO4 | 当前 PCB 的固定发射脚，不能写成 GPIO16/17 |
| 红外接收 | GPIO23 | 协议学习时开启 |
| WS2812B | GPIO2 | 状态指示灯 |
| AHT20 SDA/SCL | GPIO21 / GPIO22 | I2C 温湿度传感器 |
| BOOT/按键 | GPIO0 | ESP32 启动脚；源码用长按 3 秒触发恢复出厂，硬件操作需谨慎 |
| Serial2 预留 | GPIO16 / GPIO17 | 不是红外发射脚 |

引脚的唯一配置文件是 [`src/PinConfig.h`](src/PinConfig.h)。

## 二、问题复盘与当前处理

### 1. 红外发送与通信任务竞争

**现象**：BLE 或 MQTT 控制时偶发不发射、空调无反应，或连续调温时丢帧。

**根因**：红外载波时序敏感，不能在 BLE 回调、MQTT 回调或网页请求中直接调用 `ac.sendAc()`；连续控制还会在短时间内堆积多帧请求。

**当前处理**：所有入口统一调用 `IrManager::send()`。`IrManager` 在 Core 1 建立独立 FreeRTOS 任务，使用约 180 ms 防抖/合并，只发送最后一次稳定请求。红外发射脚统一来自 `IR_TX_PIN`，当前为 GPIO4。

**注意**：调试时应同时看 `irReq`、`irTx` 和 `irOverwrite` 计数；`irOverwrite` 增长表示控制端发送过快，并不一定是红外硬件故障。

### 2. 红外接收器未初始化就关闭导致崩溃

**现象**：开机或空闲关闭接收器时出现 `Guru Meditation Error: LoadProhibited`，设备循环重启。

**根因**：`IRremoteESP8266` 在尚未 `enableIRIn()` 时调用 `disableIRIn()`，某些版本会访问未初始化的接收对象。

**当前处理**：通过 `irReceiverEnabled` 记录状态；只有学习期间调用 `irEnableRecv()`，并且只有已开启时才调用 `irDisableRecv()`。平时默认关闭接收器，避免无意义占用 CPU。

### 3. 传感器日志刷屏

**现象**：串口持续打印相同温湿度，影响调试和主循环可读性。

**根因**：传感器按秒读取，但没有对日志做变化过滤。

**当前处理**：AHT20 仍按秒采样并更新缓存；只有温度变化达到约 0.2 ℃或湿度变化达到约 0.5% 时才输出变化日志。单次读取无效时保留上次有效值；初始化失败会输出日志。

### 4. SPIFFS 初始化顺序导致定时任务丢失

**现象**：重启后提示“已加载 0 个定时任务”，但之前明明保存过任务。

**根因**：`TimerManager::begin()` 读取 `/timers.txt` 时，SPIFFS 尚未挂载。

**当前处理**：启动顺序固定为：`SPIFFS.begin(true)` → `otaManager.begin()` → `timerManager.begin()`。定时任务存储在 `/timers.txt`，最多 10 条。

### 5. 定时任务编辑、掉电恢复和一次性任务

**现象**：编辑任务后旧任务消失，或开机任务被显示成关机；一次性任务执行后仍反复执行。

**根因**：旧客户端曾用“删除再添加”模拟编辑，并把固件返回的布尔值和字符串 `on/off` 混用。

**当前处理**：

- 使用 `timer=update;id=...` 原地更新任务，不改变任务 ID。
- 文件保存 `power/enabled/repeat` 的布尔状态，客户端必须按布尔值或明确转换后的 `on/off` 处理。
- 一次性任务执行后自动禁用；重复任务每天执行。
- WiFi 模式使用 `ntp.aliyun.com` 同步东八区时间；BLE 兼容模式可用 `time=YYYY-MM-DD HH:MM:SS` 校时。

### 6. WiFi 配网、断线和热点恢复

**现象**：首次启动无法配网、扫描时网页卡住，或短暂断网后设备长期离线。

**根因**：AP/STA 模式切换会中断配网页；同步扫描会阻塞 HTTP；没有退避的重连会反复抢占单射频。

**当前处理**：

- 固定使用 `WIFI_AP_STA`，热点广播由 `softAP/softAPdisconnect` 控制。
- 无凭据时直接开启热点；热点名为 `ESP32AC_<12 位小写设备号>`，配网地址为 `192.168.4.1`。
- WiFi 扫描使用异步流程，前端轮询扫描结果。
- STA 重连按 5/10/20/30 秒退避；断线约 30 秒仍未恢复时再次开启配网热点。
- 连上 WiFi 后关闭配网热点，避免 AP 与 STA 长期共存拖累连接。
- 凭据保存于 SPIFFS 的 `/wifi.json`。

### 7. MQTT 主题、在线状态和消息长度

**现象**：多个设备互相收到指令、面板显示旧在线状态，或异常长消息导致内存压力。

**根因**：主题没有按设备唯一标识生成；在线状态不是 retained；输入消息长度没有上限。

**当前处理**：

- 默认 Broker 为 `broker-cn.emqx.io:1883`，主题前缀为 `ac/esp32/<12 位小写设备号>`。
- 指令、响应、状态分别使用 `<base>/in`、`<base>/out`、`<base>/status`。
- `/status` 每 10 秒发布一次 retained JSON；MQTT 遗嘱发布 `{"online":false}`。
- MQTT 输入超过 512 字节直接拒绝；正常控制指令应保持短小。
- MQTT 收到的文本指令复用 `BleManager::handleCommand()`，因此 BLE 命令解析改动必须同时验证 MQTT。

**安全限制**：默认公共 Broker 仅适合开发和演示，当前配置没有默认账号密码和 TLS。正式部署应改用私有 Broker、账号和访问控制。

### 8. BLE 单包限制与长消息分片

**现象**：定时任务列表或较长响应在微信小程序中被截断。

**根因**：BLE 默认 MTU 下单次通知通常只能承载约 20 字节。

**当前处理**：`BleManager::sendChunked()` 按 18 字节分片，片段之间间隔约 10 ms，最后发送 `\n` 作为完整消息结束标记。小程序端必须按换行符重组消息，再解析 `timers=` 等 JSON。

**边界**：这是 BLE 兼容链路的约束；WiFi/MQTT 主线不应假设 MQTT 响应也按 BLE 分片。

### 9. BLE 与 MQTT 共用命令处理

**现象**：删除或重命名 BLE 代码后，MQTT 面板控制失效。

**根因**：当前架构没有复制两套空调指令解析器；MQTT 回调会把文本转交给 `bleManager.handleCommand()`。

**当前处理**：保留 `BleManager.cpp/.h`，把它视为共享命令处理模块；BLE 传输在 `WIFI_ONLY` 构建中被编译裁剪，但命令入口仍由 MQTT 使用。修改命令格式时要同时验证 BLE、小程序和 MQTT。

### 10. HomeKit 与自定义 WiFi 管理冲突

**现象**：HomeKit 初始化后配网热点起不来，或出现额外的 HomeSpan 配网 SSID。

**根因**：HomeSpan 自带 WiFi 状态机与项目的 `WifiManagerEx` 同时管理同一无线射频。

**当前处理**：WiFi 连接成功后才延迟初始化 HomeSpan；配网门户活动时暂停 `homeSpan.poll()`，由 `WifiManagerEx` 独占配网、重连和 AP/STA 状态。

### 11. OTA 下载阻塞 MQTT 心跳

**现象**：开始 OTA 后 MQTT 掉线、设备看门狗复位，或进度长期不更新。

**根因**：在 MQTT 回调里同步下载会长时间阻塞主循环。

**当前处理**：WiFi 主线采用两步流程：`ota=check` 检查版本，用户确认后 `ota=go`；下载由独立 OTA 任务和主循环分片处理，按阶段发布 `ota=progress <percent>`，完成返回 `ota=ok`，失败返回 `ota=fail:<reason>`。

**注意**：OTA 依赖 WiFi 和 MQTT；下载期间不要断电。仓库只保留设备端 OTA 实现和清单，不包含自动发布、自动推送 GitHub 或自动测试脚本。

### 12. GPIO 接线与烧录地址错误

**现象**：红外始终无效，或手动烧录后出现 `invalid header` 并循环重启。

**根因**：把红外发射接到了预留的 GPIO16/17，或只上传应用文件且烧录地址不正确。

**当前处理**：当前 PCB 的红外发射必须接 GPIO4。正常使用 PlatformIO 烧录；如必须手动烧录，应以当前构建产物和分区表给出的地址为准，应用固件不能随意写到错误偏移。

### 13. GPIO0 误触发恢复出厂

**现象**：设备启动异常、按键操作后 WiFi/定时配置消失。

**根因**：GPIO0 同时是 ESP32 启动配置脚，且当前源码把它作为按键输入；低电平持续或上电时误触发会影响启动判断。

**当前处理**：按键逻辑要求先观察到释放状态；按住约 1.5 秒显示提示，持续 3 秒才执行恢复出厂。恢复出厂只删除已知配置文件（协议、定时、WiFi）并清理 NVS 后重启。

**硬件注意**：GPIO0 属于启动脚，烧录或上电时不要强制拉低；如修改 PCB，应将普通功能键迁移到非启动脚。

## 三、当前架构边界与已知限制

1. `esp32_wifi` 是当前发布主线；不要把 `esp32dev` 或 `esp32_ble` 的 BLE 启动行为描述成 WiFi 主线行为。
2. BLE 和微信小程序是兼容链路，不是 WiFi 主线的必选依赖，但 BLE 命令处理模块不能直接删除。
3. 设备仅支持 2.4 GHz WiFi；公共 MQTT Broker 不适合生产环境。
4. MQTT、HomeKit、红外和定时任务共享同一个 ESP32 主循环，新增功能应避免长时间阻塞。
5. 任何控制入口都应通过 `IrManager::send()`，禁止在回调中直接调用 `ac.sendAc()`。
6. 修改协议或响应格式时，至少验证：MQTT 面板、BLE 小程序、定时任务、OTA 状态回包。

## 四、现场调试检查清单

### 红外无反应

- 确认编译的是目标环境，且红外发射线接在 GPIO4。
- 查询日志中的 `protocol` 是否为目标空调协议。
- 检查 `irReq` 是否增长；若增长而 `irTx` 不增长，重点检查防抖任务和任务栈。
- 检查红外接收头是否只在学习期间工作，避免把“未学习到协议”误判为发射故障。

### WiFi/MQTT 不在线

- 首次配置连接 `ESP32AC_<设备号>`，访问 `192.168.4.1`。
- 确认路由器提供 2.4 GHz 网络，并观察退避重连日志。
- 核对面板使用的 Broker、端口和主题前缀是否与设备一致。
- 先看 `/status` 是否为 `online:true`，再判断控制指令是否执行。

### 定时任务不执行

- 确认启动日志显示 SPIFFS 挂载成功且任务数量正确。
- WiFi 模式等待 NTP 同步；BLE 兼容模式先发送 `time=YYYY-MM-DD HH:MM:SS`。
- 检查任务的 `enabled`、`repeat`、`power` 字段，避免把布尔值当成字符串。
- 确认定时任务触发后红外请求计数增加。

### OTA 失败

- 确认设备已连接 WiFi 和 MQTT。
- 先执行 `ota=check`，只有返回 `ota=found <version>` 后再执行 `ota=go`。
- 检查清单版本是否高于 `FW_VERSION`，下载地址是否可访问。
- OTA 期间保持供电，失败时保留当前固件并根据 `ota=fail:<reason>` 排查。

## 五、常用构建命令

```powershell
# 当前 WiFi/MQTT/HomeKit/OTA 主线
pio run -e esp32_wifi
pio run -e esp32_wifi -t upload

# 兼容构建：BLE + WiFi/HomeKit，可从 BLE 切换 WiFi
pio run -e esp32dev

# 兼容构建：仅 BLE，不包含 WiFi/MQTT
pio run -e esp32_ble
```

详细日志开关见 `src/Debug.h`。修改后请先使用 `git diff --check` 检查文档格式，并确认提交只包含本文件。

---

文档依据：当前 `src/`、`platformio.ini`、`README.md` 和 `OTA流程说明.md`。最后更新：2026-09-10。
