# ESP32 空调控制器项目问题总结

## 项目概述

本项目是一个基于 ESP32 的空调红外控制器，支持 WiFi 和 BLE 两种连接方式，可通过微信小程序 / uni-app 双端 App 控制。V2.0 新增：BLE 分包协议、手机校时定时、双固件版本（苹果版 / 安卓纯 BLE 版）、双端 App、断线重连等。

---

## 问题一：核心资源争用导致BLE模式下红外发送失败

### 问题描述

在BLE模式下连接微信小程序后，红外发射功能无法正常工作，空调没有反应。但在WiFi模式下红外发射正常。

### 问题原因

ESP32是双核处理器（核心0和核心1）：

- BLE协议栈默认运行在**核心0**
- 之前的代码在BLE回调中直接调用 `ac.sendAc()` 发送红外信号
- 这导致红外发送也在核心0上执行，与BLE栈争用CPU资源
- 当BLE通信繁忙时，红外发送时序被打乱，导致发送失败

### 解决方案

将所有红外发送操作统一通过 `IrManager::send()` 方法，在核心1的专用FreeRTOS任务中执行：

```cpp
// IrManager.cpp - 红外任务固定在核心1
void IrManager::begin() {
    xTaskCreatePinnedToCore(
        irTaskFunction,    // 任务函数
        "IR_Tx_Task",      // 任务名称
        4096 + 1028,       // 栈大小
        NULL,              // 参数
        1,                 // 优先级
        &irTaskHandle,     // 任务句柄
        1                  // 核心1
    );
}
```

### 修改文件

1. **BleManager.cpp** - 改用 `irManager.send()` 而非直接调用 `ac.sendAc()`
2. **main.cpp** - `AC_SET_DATA` 函数添加 `power` 参数
3. **WifiManagerEx.cpp** - 更新函数声明

---

## 问题二：传感器数据频繁刷新

### 问题描述

串口输出 `传感器读取: 温度=XX.X℃, 湿度=XX.X%` 一直刷新，日志刷屏。

### 问题原因

传感器读取逻辑没有判断数据是否变化，每次读取都输出日志。

### 解决方案

添加数据变化检测，只有当温度变化≥0.1℃或湿度变化≥0.5%时才输出日志：

```cpp
// SensorManager.cpp
bool changed = false;
if (abs(temperature - lastTemp) >= 0.1f) {
    changed = true;
    lastTemp = temperature;
}
if (abs(humidity - lastHumidity) >= 0.5f) {
    changed = true;
    lastHumidity = humidity;
}
if (changed) {
    Serial.printf("传感器读取: 温度=%.1f℃, 湿度=%.1f%%\n", temperature, humidity);
}
```

---

## 问题三：BLE温湿度数据不显示

### 问题描述

微信小程序连接后，温湿度数据不显示。

### 问题原因

BLE连接成功后没有立即发送温湿度数据，且主循环中没有调用发送函数。

### 解决方案

1. BLE连接回调中立即发送当前温湿度
2. 主循环中调用 `sendEnvironmentDataIfNeeded()` 定期发送

```cpp
// BleManager.cpp - 连接回调
void onConnect(BLEServer *pServer) override {
    parent->deviceConnected = true;
    ledManager.stopBlink();
    ledManager.setColor(CRGB::Blue);
    parent->sendTempHumidity(envTemperature, enHumidity); // 立即发送
}

// main.cpp - 主循环
if (isBLEMode) {
    bleManager.loop();
    sendEnvironmentDataIfNeeded(); // 定期发送
}
```

---

## 问题四：LED灯光逻辑不正确

### 问题描述

- WiFi模式下应该闪绿灯，但灯不闪
- 学习模式应该显示紫灯，但显示蓝灯
- 红外发射时灯颜色需要改变

### 解决方案

实现完整的 `LedManager` 类，支持颜色设置和闪烁：

| 状态         | LED颜色 | 模式 |
| ------------ | ------- | ---- |
| BLE等待连接  | 蓝色    | 闪烁 |
| BLE已连接    | 蓝色    | 常亮 |
| WiFi等待连接 | 绿色    | 闪烁 |
| WiFi已连接   | 绿色    | 常亮 |
| 红外学习     | 紫色    | 常亮 |
| 红外发射     | 红色    | 闪烁 |

---

## 问题五：模式切换隔离问题

### 问题描述

切换模式时，之前的模式没有完全关闭，导致资源冲突。

### 解决方案

添加模式标志和切换逻辑：

```cpp
// 切换到WiFi模式
if (requestSwitchToWiFi) {
    bleManager.disable();  // 先关闭BLE
    isBLEMode = false;
    isWiFiMode = true;
    wifiManager.enable();  // 再开启WiFi
}

// 切换到BLE模式
if (requestSwitchToBLE) {
    wifiManager.disable(); // 先关闭WiFi
    isBLEMode = true;
    isWiFiMode = false;
    bleManager.enable();   // 再开启BLE
}
```

---

## 问题六：小程序命令处理不匹配

### 问题描述

小程序发送的命令格式与ESP32处理逻辑不匹配，导致红外发射参数错误。

### 小程序命令格式

| 操作     | 命令格式                          |
| -------- | --------------------------------- |
| 打开空调 | `temp=25;mode=0;speed=0;power=on` |
| 关闭空调 | `power=off`                       |
| 调节参数 | `temp=X;mode=X;speed=X;power=on`  |

### 解决方案

更新BLE命令解析逻辑：

```cpp
// 关机
if (command == "power=off") {
    irManager.send(ac.next.degrees, (int)ac.next.fanspeed, (int)ac.next.mode, false);
}

// 开机/调参数
if (command.startsWith("temp=")) {
    int temp = command.substring(5, idx1).toInt();
    int mode = command.substring(idx1+6, idx2).toInt();
    int speed = command.substring(idx2+6, idx3).toInt();
    irManager.send(temp, speed, mode, true);
}
```

---

## 问题七：BLE 单包 20 字节限制导致回包截断

### 问题描述

定时任务列表、添加/删除任务等回包超过 20 字节被截断，App 永远收不到完整响应（如 `timer_add=success;id=` 被截断），任务管理“用不了”。

### 问题原因

BLE 默认 MTU 23，单包通知最多 20 字节；固件一次性 `setValue + notify()` 发送整串文本，超长部分被截断。

### 解决方案

- 固件新增 `sendChunked()`：按 ≤18 字节分包发送，末尾追加 `\n` 作为帧结束标记
- App 端按 `\n` 分帧重组，`timers=` 的 JSON 必须能完整解析才消费，兼容旧固件的无分隔短消息

---

## 问题八：BLE 模式下定时任务不执行

### 问题描述

手机断开后，设置的定时任务到点没有执行，空调无反应。

### 问题原因

1. 主循环里 `timerManager.loop()` 只在 WiFi 模式调用，BLE 模式从不检查时间
2. 设备时间依赖 NTP（需连接 WiFi），BLE 模式下 `time` 查询一直返回“未同步”

### 解决方案

- 主循环 BLE 模式也调用 `timerManager.loop()`
- 新增 `time=YYYY-MM-DD HH:MM:SS` 校时命令，`TimerManager::setTimeFromPhone()` 把手机时间写入 ESP32 RTC，设备离线也能准点执行

---

## 问题九：定时任务重启后丢失

### 问题描述

保存的定时任务在设备重启后“已加载 0 个定时任务”。

### 问题原因

`timerManager.begin()` 在 `SPIFFS.begin()` 之前执行，文件系统未挂载，读取失败。

### 解决方案

调整初始化顺序：SPIFFS 挂载成功后再调用 `timerManager.begin()`。

---

## 问题十：uni-app 迁移后协议/模式/风速弹窗打不开

### 问题描述

迁移到 uni-app 后，点击协议、模式、风速没有反应。

### 问题原因

组件里布尔数据与同名方法冲突（如 `showProtocolPicker` 既是 data 又是方法），Vue 中 data 会覆盖方法。

### 解决方案

打开弹窗的方法统一改名为 `openXxx` / `hideXxx`，模板绑定同步更新；定时页的“添加任务”弹窗同样处理。

---

## 问题十一：开机任务显示成关机

### 问题描述

设置“开机”的定时任务在列表里显示“关机”，编辑保存后还会被真改成关机。

### 问题原因

固件返回的任务 `power` 是布尔值（`true`/`false`），App 端用字符串 `'on'`/`'off'` 比较，判断全部失效。

### 解决方案

统一用 `isTaskPowerOn`（`true` 或 `'on'` 都算开机），编辑时把布尔值转成 `'on'`/`'off'` 再保存。

---

## 问题十二：编辑任务“删旧加新”造成误解

### 问题描述

修改任务时间后原任务消失，用户误以为“重新添加会删掉原来的”。

### 问题原因

固件没有更新命令，App 用“先删除、再添加”实现编辑。

### 解决方案

固件新增 `timer=update;id=X;...` 命令，`TimerManager::updateTask()` 原地修改任务，不再删除重建；App 编辑直接发送 update。

---

## 问题十三：安卓断线后重连不上

### 问题描述

设备断开后自动重连失败，手动连接也被“连接尝试次数过多”挡住。

### 问题原因

1. 连接失败次数未清零，后续手动连接被上限拦截
2. 连接超时重试与失败重试两条链路叠加
3. 重连前蓝牙适配器未重新初始化

### 解决方案

- 失败放弃与手动连接时清零尝试次数
- 单一重试链：3s/6s/9s 递增，最多 3 轮
- 连续失败后先重置蓝牙适配器（`closeBluetoothAdapter` + `openBluetoothAdapter`）再重连

---

## 问题十四：多页面并发写 BLE 报 10007

### 问题描述

首页与定时页同时写特征值时，偶发 `writeBLECharacteristicValue:fail property not support (10007)`。

### 问题原因

两个页面各自维护发送队列，Android 并发写同一个 GATT 特征导致冲突。

### 解决方案

App 增加全局串行写队列（`enqueueWrite`），任意时刻只发送一条命令。

---

## 问题十五：定时页显示“未连接”

### 问题描述

首页已连接设备，进入定时任务页却显示“未连接”。

### 问题原因

定时页再次创建连接，与首页连接冲突（Android 同一设备双连接失败）。

### 解决方案

全局共享连接状态（`getApp().globalData.ble`），定时页复用首页连接；只有自建连接时才负责断开，退出时只清理自己的监听。

---

## 问题十六：开机调用 irrecv.disableIRIn() 导致崩溃

### 问题描述

优化“空闲关闭红外接收”后，设备开机即 `Guru Meditation Error: LoadProhibited`，反复重启。

### 问题原因

IRremoteESP8266 的 `disableIRIn()` 在接收器从未 `enableIRIn()` 时解引用空指针（`EXCVADDR=0x1`）。

### 解决方案

开机不调用任何红外开关（接收器默认关闭即可），仅在协议学习时 `enableIRIn()` / 学习结束 `disableIRIn()`。

---

## 问题十七：网页工具烧录地址错误导致启动循环

### 问题描述

用网页烧录工具上传固件后，设备反复打印 `invalid header: 0x554e203d` 并重启。

### 问题原因

只上传了 `firmware.bin` 且地址错误（应用必须写在 `0x10000`），引导程序在应用分区读到文本数据。

### 解决方案

用 PlatformIO 完整烧录四个文件：`bootloader.bin`(0x1000)、`partitions.bin`(0x8000)、`boot_app0.bin`(0xe000)、`firmware.bin`(0x10000)。

---

## 关键技术点总结

### 1. ESP32双核任务分配

```
核心0: BLE协议栈、WiFi协议栈、Arduino主循环
核心1: 红外发送任务（时序敏感）
```

### 2. 线程安全

- 使用 `portMUX_TYPE` 临界区保护共享变量
- 使用 `xSemaphoreHandle` 互斥锁保护BLE特征值操作

### 3. FreeRTOS任务

```cpp
xTaskCreatePinnedToCore(
    taskFunction,   // 任务函数
    "TaskName",     // 名称
    stackSize,      // 栈大小
    NULL,           // 参数
    priority,       // 优先级
    &taskHandle,    // 句柄
    coreID          // 绑定的核心
);
```

---

## 文件结构

```
src/
├── main.cpp           // 主程序入口、HomeKit集成
├── BleManager.cpp/h   // BLE通信管理
├── WifiManagerEx.cpp/h// WiFi连接和Web服务器
├── IrManager.cpp/h    // 红外发射和学习
├── SensorManager.cpp/h// AHT20温湿度传感器
├── TimerManager.cpp/h // 定时任务管理、RTC校时
├── LedManager.cpp/h   // LED状态指示
└── Debug.h            // 详细日志开关（#define DEBUG_LOG）
```

---

## 编译命令

```bash
pio run -e esp32dev            # 苹果版（HomeKit/WiFi）
pio run -e esp32_ble           # 安卓纯BLE版（无WiFi/HomeKit）
pio run -e esp32dev -t upload  # 编译并上传
```

详细日志默认开启；想关闭时把 `src/Debug.h` 里的 `#define DEBUG_LOG` 注释掉再编译。

---

_文档生成时间：2026-04-04（V2.0 更新：2026-08-21）_

