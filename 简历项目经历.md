# 核心项目经历

## 基于 ESP32 的智能红外空调控制器（BLE + HomeKit + 双端 App）

**项目周期**：2025.03–2025.06（V1.0），2026.08 完成 V2.0 全面迭代

**核心硬件**：ESP32-WROOM-32 主控、HS0038 红外接收头、红外发射管、AHT20 温湿度传感器、WS2812B RGB 状态灯

**技术栈**：立创 EDA、PlatformIO / Arduino、FreeRTOS、IRremoteESP8266、BLE GATT、WiFi STA、HomeKit（HomeSpan）、微信小程序原生开发、uni-app / Vue3（双端 App）

### 个人负责

**硬件设计与调试**

- 独立完成 2 层 PCB 设计，包含电源稳压、红外收发电路、AHT20 I2C 传感器接口、WS2812B 状态指示灯电路
- 完成打样、焊接与硬件调试，优化天线布局确保 BLE 通信稳定性

**嵌入式软件开发（V1.0）**

- 基于 FreeRTOS 搭建多任务系统：拆分红外发射、温湿度采集、BLE/WiFi 通信、状态指示、定时任务等独立任务
- 红外发射任务固定在 Core 1，BLE 协议栈运行在 Core 0，用互斥锁与临界区解决资源竞争，保证红外时序不被 BLE 通信打断
- 集成 IRremoteESP8266，支持 80+ 主流空调品牌的红外协议解码与发射
- 实现红外码值学习，自动识别协议并持久化到 SPIFFS
- 集成 Apple HomeKit（HomeSpan），支持 Siri 语音控制与家庭自动化场景

**通信与协议设计（V2.0）**

- 设计纯文本 BLE 命令协议，覆盖查询、控制、协议学习、定时任务、校时、模式切换
- 解决 BLE 单包 20 字节限制：固件端分包发送（≤18 字节 + `\n` 帧），App 端按帧重组，长列表与长命令可靠传输
- 双固件版本方案：苹果版（保留 HomeKit/WiFi/网页控制）与安卓纯 BLE 版（`BLE_ONLY` 宏裁剪，RAM 14.1%、Flash 50.5%），一套源码编译区分
- 手机校时方案：BLE 模式下手机把时间写入 ESP32 RTC，设备离线也能准点执行定时任务，不再依赖 WiFi/NTP

**双模通信架构设计**

- **BLE 模式**：微信小程序 / 双端 App 近场控制，支持设备发现、温湿度实时显示、空调控制、定时任务管理、红外学习
- **WiFi 模式**：集成 Apple HomeKit 智能家居生态，支持 Siri 语音控制与家庭自动化场景
- 实现一键模式切换（App 按钮 / BOOT 键 / HomeKit 开关），BLE 与 WiFi 模式互斥运行，避免资源冲突

**定时任务系统**

- BLE 模式下由手机校时写入 ESP32 RTC，设备本地执行，不依赖 WiFi/NTP，断开手机也能准点触发
- 定时任务持久化存储至 SPIFFS，断电不丢失；支持开机/关机、温度/模式/风速、单次/每日重复，最多 10 条
- 固件支持 `timer=add / update / delete / enable` 完整命令集，编辑任务原地更新，不再删除重建

**双端 App 开发（V2.0，uni-app 迁移）**

- 将微信小程序迁移为 uni-app / Vue3，一套代码编译 Android / iOS / 微信小程序
- 重构蓝牙连接为状态机（未连接/扫描/连接中/已连接/重连中），支持设备列表按信号排序、最近设备一键重连、权限与排查引导
- 全局串行写队列解决多页面并发写 BLE 特征值冲突；首页与定时页共享连接状态，避免重复连接
- 断线自动重连（递增等待 + 适配器重置），失败次数清零保证手动重连可用

**性能与稳定性优化**

- 温湿度 1 秒采样 + 阈值触发发送，平衡实时性与功耗
- 状态灯指示：蓝色(BLE)、绿色(WiFi)、紫色(学习)、红色闪烁(发射)
- 详细日志开关（`DEBUG_LOG`），默认关闭噪音日志、可一键开启调试
- 空闲关闭红外接收、连接后停止 BLE 广播、广播间隔 1~1.5s，降低空闲功耗

### 项目成果

- 控制器稳定运行，支持 10+ 主流空调品牌，小程序/App 均可替代传统红外遥控器
- V2.0 双固件版本：安卓纯 BLE 版 Flash 占用从 77.9% 降至 50.5%，RAM 从 22.9% 降至 14.1%
- 集成 Apple HomeKit，支持 Siri 语音控制与家庭自动化
- 硬件与代码开源至个人 GitHub，并打 v1.0 / v2.0 版本标签便于对比维护

### 技术亮点

| 模块 | 技术方案 |
| --- | --- |
| 多任务调度 | FreeRTOS 任务分离，Core 0 运行 BLE，Core 1 运行红外发射 |
| 数据持久化 | SPIFFS 存储协议配置、定时任务，断电保持 |
| BLE 通信 | 分包发送 + `\n` 帧重组，突破单包 20 字节限制 |
| 校时与定时 | 手机校时写入 RTC，BLE 离线也能准点执行定时任务 |
| 双端 App | uni-app 一套代码编译 Android/iOS/小程序，连接状态机化 |
| 双固件方案 | `BLE_ONLY` 宏裁剪，一套源码产出苹果版与安卓精简版 |
| 红外协议 | 支持学习模式，自动识别 80+ 红外协议 |
| 智能家居 | HomeKit 集成，支持 Siri 语音控制，新增模式切换开关 |

---

## BLE 通信协议设计

| 命令 | 功能 | 响应格式 |
| --- | --- | --- |
| `status` | 查询温湿度 | `t30.5h45.0` |
| `power` | 查询电源状态 | `power=on` |
| `temp=25;mode=1;speed=1;power=on` | 开机/调温/模式/风速 | `temp=25;mode=1;speed=1;power=on` |
| `power=off` | 关机 | `power=off` |
| `protocol=KELVINATOR` | 设置协议 | `protocol=KELVINATOR` |
| `learn=start` | 红外学习 | `learn=success:KELVINATOR` |
| `time=YYYY-MM-DD HH:MM:SS` | 手机校时 | `time=ok` |
| `timer=list` | 查询定时任务 | `timers=[{...}]` |
| `timer=add/update/delete/enable` | 定时任务管理 | `timer_xxx=success;id=N` |
| `wifi_mode` | 切换 WiFi 模式 | `switching=wifi` / `wifi=unsupported` |

---

## 项目架构图

```
┌───────────────────────────────────────────────────────────────┐
│  双端 App（uni-app：Android / iOS） + 微信小程序               │
│  连接状态机 / 空调控制 / 定时任务 / 协议学习 / 校时            │
└──────────────────────────┬────────────────────────────────────┘
                           │ BLE GATT（分包 + \n 帧）
                           ▼
┌───────────────────────────────────────────────────────────────┐
│                    ESP32 主控（FreeRTOS）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ BLE 任务 │ │ 红外任务 │ │ 传感器   │ │ 定时任务 │         │
│  │ Core 0   │ │ Core 1   │ │ 1s采样   │ │ RTC执行  │         │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │
│  AHT20 温湿度 / 红外发射 / HS0038 接收 / WS2812B 状态灯       │
└──────────────────────────┬────────────────────────────────────┘
                           │ WiFi（仅苹果版）
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Apple HomeKit（HomeSpan）+ Web Server :8080 + OTA            │
└───────────────────────────────────────────────────────────────┘
```
