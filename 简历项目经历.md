# 核心项目经历

## 基于 ESP32 + 微信小程序的智能红外空调控制器（个人独立全栈项目）

**项目周期**：2025.03–2025.06

**核心硬件**：ESP32-WROOM-32 主控、HS0038 红外接收头、红外发射管、AHT20 温湿度传感器、WS2812B RGB 状态灯

**技术栈**：立创 EDA、Arduino/ESP32、FreeRTOS、IRremoteESP8266、BLE GATT、WiFi STA、HomeKit、微信小程序原生开发

### 个人负责

**硬件设计与调试**

- 独立完成 2 层 PCB 设计，包含电源稳压、红外收发电路、AHT20 I2C 传感器接口、WS2812B 状态指示灯电路
- 完成打样、焊接与硬件调试，优化天线布局确保 BLE 通信稳定性

**嵌入式软件开发**

- 基于 FreeRTOS 搭建多任务系统：拆分红外解码、温湿度采集、BLE/WiFi 通信、状态指示、定时任务 5 个独立任务
- 通过消息队列传递控制指令与传感器数据，信号量实现任务同步，解决 BLE 与红外发射的资源竞争问题
- 集成 IRremoteESP8266 库，支持格力、美的、海尔、KELVINATOR 等主流品牌空调的红外协议解码与发射
- 实现红外码值学习功能，支持自定义红外协议的捕获与存储

**双模通信架构设计**

- **BLE 模式**：微信小程序近场控制，支持设备发现、温湿度实时显示、空调控制、定时任务管理、红外学习
- **WiFi 模式**：集成 Apple HomeKit 智能家居生态，支持 Siri 语音控制与家庭自动化场景
- 实现一键模式切换，BLE 与 WiFi 模式互斥运行，避免资源冲突

**定时任务系统**

- 基于 NTP 网络时间同步，实现精准定时开关空调功能
- 定时任务持久化存储至 SPIFFS 文件系统，断电不丢失
- 支持一次性任务与每日重复任务，最多存储 10 个定时任务

**微信小程序开发**

- 独立开发配套微信小程序，包含设备绑定、实时温湿度显示、空调开关/温度/模式/风速调节
- 实现定时任务管理（添加/删除/启用/禁用）、红外码值学习、协议切换、WiFi 模式切换等功能
- 优化 BLE 数据传输协议，解决安卓设备 MTU 限制问题，确保数据完整性

**性能优化**

- 优化温湿度采集策略，1 秒采样间隔 + 阈值触发发送，平衡实时性与功耗
- 设计状态灯指示系统：蓝色(BLE连接)、绿色(WiFi连接)、紫色(红外学习)、红色闪烁(红外发射)
- 待机电流 < 15mA，支持长时间稳定运行

### 项目成果

- 控制器稳定运行 3 个月以上，支持 10+ 主流空调品牌
- 小程序已上线使用，可替代传统红外遥控器
- 集成 Apple HomeKit，实现智能家居生态互联
- 硬件与代码均已开源至个人 GitHub

### 技术亮点

| 模块         | 技术方案                                                |
| ------------ | ------------------------------------------------------- |
| 多任务调度   | FreeRTOS 任务分离，Core 0 运行 BLE，Core 1 运行红外发射 |
| 数据持久化   | SPIFFS 存储协议配置、定时任务，断电保持                 |
| BLE 通信优化 | 紧凑数据格式（t30.5h45.0），适配安卓 MTU 限制           |
| 红外协议     | 支持学习模式，自动识别 80+ 红外协议                     |
| 智能家居     | HomeKit 集成，支持 Siri 语音控制                        |

---

## BLE 通信协议设计

| 命令          | 功能         | 响应格式                   |
| ------------- | ------------ | -------------------------- |
| `status`      | 查询温湿度   | `t30.5h45.0`               |
| `power`       | 查询电源状态 | `power=on`                 |
| `set_temp=26` | 设置温度     | `temp=26`                  |
| `set_mode=1`  | 设置模式     | `mode=1`                   |
| `learn`       | 红外学习     | `learn=success:KELVINATOR` |
| `timer=list`  | 查询定时任务 | `timers=[{...}]`           |
| `wifi_mode`   | 切换WiFi模式 | `switching=wifi`           |

---

## 项目架构图

```
┌─────────────────────────────────────────────────────────┐
│                    微信小程序                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 设备绑定  │ │ 温湿度显示│ │ 空调控制  │ │ 定时任务  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
└─────────────────────────┬───────────────────────────────┘
                          │ BLE GATT
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    ESP32 主控                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │              FreeRTOS 多任务系统                   │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐     │  │
│  │  │BLE任务 │ │红外任务│ │传感器任务│ │定时任务│     │  │
│  │  │Core 0  │ │Core 1  │ │Core 1  │ │Core 1  │     │  │
│  │  └────────┘ └────────┘ └────────┘ └────────┘     │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐          │
│  │AHT20   │ │红外发射│ │红外接收│ │WS2812B │          │
│  │温湿度   │ │LED     │ │HS0038  │ │状态灯  │          │
│  └────────┘ └────────┘ └────────┘ └────────┘          │
└─────────────────────────┬───────────────────────────────┘
                          │ WiFi
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 Apple HomeKit / Web Server              │
└─────────────────────────────────────────────────────────┘
```
